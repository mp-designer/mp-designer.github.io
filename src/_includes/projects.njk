<div class="grid gap-20 grid-cols-1">
  {% for project in collections.projects %}
    <a href="{{ project.url }}" class="group">
      <div class="mb-4 md:mb-8 overflow-hidden">
        <img src="{{ project.data.imgSrc }}" class="object-cover w-full aspect-square md:aspect-[696/540] lg:aspect-[1200/540] group-hover:scale-110 transition-all ease-out duration-300" alt="Illustration of {{ project.data.title }}" style="view-transition-name: project-image-{{project.data.index}}"/>
      </div>
      <div class="flex flex-col justify-between items-start">
        <div class="max-w-[793px]">
          <h3 class="mb-4 text-grey6 font-sans-heading text-hS md:text-hM">{{ project.data.title }}</h3>
          <p class="text-bodyS md:text-bodyL">{{ project.data.description }}</p>
        </div>
      </div>
    </a>
  {% endfor %}
</div>
